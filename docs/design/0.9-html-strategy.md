# Task 0.9 design — HTML/rich-text strategy: renderer selection + sanitizer + central URI policy (A10, A11)

Status: revised after llm-pair round 1 (codex gpt-5.6-sol@max security-weighted review: `revise`, 3 critical + 10 major + 2 minor — ALL accepted; Gemini quota-unavailable). Owner: orchestrator.

## Renderer selection: `@native-html/render` (required peerDependency, A10) — conditional

Selection unchanged (actively maintained successor, SWM co-maintained, pure JS, extensible; react-native-render-html dormant, htmlview dead, @expo/html-elements not a string renderer, WebView rejected as default). Round-1 additions:
- **Stop/go gate**: acceptance = exact-package tests on RN 0.86 New Arch — Jest mount proves JS/React compat ONLY; the real gate is the 0.2 release-gate smoke app rendering the full allowlisted tag set (nesting, lists, tables, images, RTL, a11y roles) on both platforms. Failure triggers the named fallback: a spike task for a minimal internal renderer over the SAME TRE/htmlparser2 AST with a REDUCED tag set (drop tables in fallback v1 — sized separately, not assumed free).
- **The adapter ALWAYS installs `renderersProps.a.onPress`** — the lib's default anchor behavior is `Linking.openURL`, i.e. auto-navigation. With no host callback the adapter no-ops (event dropped, diagnostic in dev). Press-time revalidation: the canonical URI (from the policy, below) is re-validated at press; test with `Linking.openURL` mocked to THROW proving it is never reached for valid, invalid, and callback-absent cases. Renderer inline-CSS processing DISABLED (defense in depth; style already stripped).

## Sanitizer (A11) — `src/security/sanitize-html.ts`

**Single-parse AST pipeline (round-1 rework — no parse→serialize→reparse):** parse ONCE with the exact TRE/htmlparser2 configuration the renderer uses, filter into a PRIVATE reconstructed allowlisted AST (positive reconstruction — allowlisted nodes/attrs are COPIED into new nodes; everything else dropped, active subtrees dropped whole, never unwrapped), pass that AST to the renderer's DOM-source input. No serializer divergence, no post-validation mutation window. `htmlparser2`/`domhandler` become DECLARED dependencies (currently transitive-only — undeclared import was a defect; hoisting hid it).

- Tag allowlist as before (`p div span br hr h1-h6 ul ol li blockquote pre code strong b em i u s sub sup table thead tbody tr td th a img`). Always-dropped-as-subtrees now explicitly includes foreign/raw-text/container elements: `svg math foreignObject template noscript meta link base style script iframe object embed form input textarea select button`, comments, doctypes, CDATA, processing instructions.
- Per-tag attribute allowlist; duplicate attributes: first occurrence wins, rest dropped + diagnostic; `width/height/rowspan/colspan` validated as bounded integers (1–10000 / spans 1–1000); `style` stripped unconditionally (fidelity loss documented; future CSS support = separate allowlisted property/value parser, no `url()`, no imports — recorded, not built).
- **Resource bounds (attacker-controlled input)**: pre-parse source cap (256KB default, configurable down only), node count (5000), depth (32, enforced during parse or iterative traversal), attributes per element (16), attr value length (2048), decoded text total (512KB), images per document (32), table dimensions (200×64). Exceeding any → render as plain text extraction + diagnostic (never partial-render).
- Bypass corpus (round-1 additions): htmlparser2 implied-close/table-scope tricks, foreignObject/SVG/MathML smuggling, `template` content, entity double-decode, attr-value breakout, tab/newline/NUL-embedded schemes, uppercase/mixed schemes, protocol-relative, data: with whitespace/percent-escapes/extra params.
- **NO raw opt-out** (round-1 critical): the old `dangerouslyDisableHtmlSanitization` is gone. Trusted-content relaxation = a `relaxedFormatting` policy that may widen the FORMATTING allowlist (more tags/attrs) but an **immutable safety pass always runs**: active elements/handlers removed, URI policy enforced, renderer JS/inline-CSS off. The `javascript:` rule is now genuinely unoverridable because no path skips the safety pass.

## Central URI policy (A11) — `src/security/uri-policy.ts`

**Scheme AND origin policy** (round-1 critical: A11 says scheme/origin; schemes alone let author HTML trigger silent network fetches to arbitrary hosts):

- `validateUri(raw, context, config)` returns `{ ok: true, canonical, scheme, origin } | { ok: false, reason }` — **sinks MUST consume `canonical`** (validate-one/use-another bug closed; adapter/tests enforce). Rejection (not stripping) of embedded C0/NUL/BOM/Unicode-separator chars; lowercase-normalized scheme; parsed origin.
- **Per-context defaults**: `link` (event-only, human-mediated) = broad scheme set (`https http mailto tel`), no origin restriction. **Automatic-fetch contexts** (`image`, `background`, `choicesByUrl`, `video`) = `https:` only by default + **origin allowlist** (default: empty = same-config-declared origins only; consumers list allowed origins at `<Survey>` config). Denied by default in fetch contexts: `http:`, IP-literal hosts, `localhost`/`.local`/RFC-1918/link-local ranges, URL-embedded credentials (`user:pass@`), non-default ports unless origin-listed.
- **Immutable per-context deny set**: `javascript: vbscript: file: about: blob: filesystem: intent: content: jar: data:` (data: excepted ONLY for `image` under the strict data-image rule below) — not overridable by any config.
- **Data-image rule** (round-1 tightening): `data:image/(png|jpe?g|gif|webp);base64,<payload>` — strict base64 alphabet + padding, no whitespace/percent-escapes/extra parameters; cap on DECODED bytes (1MB, computed from encoded length pre-decode); magic-bytes check matches declared MIME before handing to the native decoder.
- `choicesByUrl` (round-1 correction — pre-model JSON validation is INSUFFICIENT: core substitutes `{variables}` at REQUEST time, choicesRestful.ts:205-228, reaching xhr.open): policy enforces at BOTH ends — (a) JSON-time lint: substitutions forbidden in scheme/authority/port positions (path/query only, encoded); (b) request-time: validate the FULLY RESOLVED URL in `settings.web.onBeforeRequestChoices` (the pre-request hook — M1 wiring task verifies it aborts before xhr.open; if the hook proves unable to abort, fallback = wrap/replace the ChoicesRestful ajax seam via documented core callback, NOT a core patch). Redirect policy: `redirect: 'follow'` disallowed cross-origin — enforced via transport options where the platform allows; documented residual risk where it doesn't.
- No auto-navigation anywhere; `navigateToUrl` family surfaces as events (M1 wiring).

## Sink-to-owner matrix (round-1: explicit, replaces prose)

| Sink | Kind | Content origin | Owner |
|---|---|---|---|
| locString `hasHtml` path (title/description/choice text/labels/errors/`emptySurveyText`) | render | host-converted markdown (host-trusted) | 1.6 LocalizableString renderer (consumes adapter) |
| `html` question `locHtml.renderedHtml` | render | author JSON | M5 html-question task |
| `processedCompletedHtml` / `completedBeforeHtml` / `loadingHtml` | render | author JSON | M1 Survey shell |
| `completedHtmlOnCondition` items | render | author JSON — **correction (round-1): resolves through plain `LocalizableString.text` in practice; literal HTML + processHtml, NOT markdown-gated. Parity test with onTextMarkdown registered proves the callback is unused here** | M1 Survey shell |
| image question / imagepicker / choice images / survey logo | auto-fetch | author JSON | M2/M4 tasks (policy context `image`) |
| background image | auto-fetch | author JSON | 0.7 owner (context `background`) |
| `choicesByUrl` | auto-fetch | author JSON + runtime variables | 0.9 policy + M1 wiring (context `choicesByUrl`) |
| expo-video sources | auto-fetch | author JSON | M5 (context `video`) |
| anchor presses inside rendered HTML | event-only | any | 0.9 adapter (context `link`) |
| `navigateToUrl(OnCondition)` | event-only | author JSON | M1 events task |
| flowpanel `content`, custom-widget `htmlTemplate` | — | — | won't-support (unchanged) |

## Sequencing (round-1 correction — one canonical split; plan-doc task rows to be amended to match)

- **0.9 implements**: `uri-policy.ts` + `sanitize-html.ts` (AST pipeline) + the secured `<SanitizedHtml>` adapter (always-installed onPress, CSS off, bounds) + dependency declarations (`@native-html/render` peer+dev; `htmlparser2`/`domhandler` declared) + ESLint boundary (static/type/subpath/require/require.resolve/dynamic-import forms — same selector family as the facade rule) + sink-level tests + DIFFERENCES entries.
- **1.1** Survey-level config wiring (origin allowlists, policy config plumbing) — plan row 1.1 amended.
- **1.6** LocalizableString renderer consuming the adapter (plan's 1.3 is the width-expression resolver — my draft misassigned it; corrected).
- Lazy loading claim narrowed: deferred module EVALUATION only (peer stays installed; Metro bundles statically discoverable requires) — measured in a packed Metro build, not claimed as bundle-size savings.

## Non-goals

No markdown parser (host-side). No style-attr CSS in v1 (stripped unconditionally). No WebView path. No iframe/YouTube. No sanitizer bypass, no raw opt-out.
